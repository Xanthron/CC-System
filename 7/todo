--TODO cant rename and create disk in root folder

--TODO replace |nil with |"nil" and change optional to nil

--TODO Scroll bar handle size is always 1

--TODO Make select variable needs focus, so when selected et gets focused (specially for input field)
--TODO Overlapping wait animation

Strip Miner:
--TODO multiple building blocks
--TODO option ignore path building when build slot empty
--TODO option ignore torch when torch 
--TODO Stop und exit does not work properly